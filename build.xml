<?xml version="1.0" encoding="iso-8859-1"?>
<project name="Build Smily" default="init-ivy" basedir="." xmlns:ivy="antlib:org.apache.ivy.ant" xmlns="antlib:org.apache.tools.ant">
	
	
	
	<property name="ivy.install.version" value="2.3.0" />
	<property name="ivy.dep.file" value="ivy/ivy.xml" />
	<property name="ivy.home" value="${user.home}/.ant" />
	<property name="ivy.jar.dir" value="${ivy.home}/lib" />
	<property name="ivy.jar.file" value="${ivy.jar.dir}/ivy.jar" />

	<taskdef resource="org/apache/ivy/ant/antlib.xml" uri="antlib:org.apache.ivy.ant" classpath="${ivy.jar.dir}/ivy.jar" />

	<target name="init-ivy" depends="download-ivy">
		<ivy:settings file="${basedir}/ivy/ivysettings.xml" />
		<ivy:retrieve conf="compile" pattern="lib/[conf]/[artifact](-[classifier]).[ext]"/>
		<ivy:retrieve conf="test" pattern="lib/[conf]/[artifact](-[classifier]).[ext]"/>
	</target>

	<target name="download-ivy">
		<mkdir dir="${ivy.jar.dir}" />
		<get src="http://repo1.maven.org/maven2/org/apache/ivy/ivy/${ivy.install.version}/ivy-${ivy.install.version}.jar" dest="${ivy.jar.file}" usetimestamp="true" />
	</target>

</project>